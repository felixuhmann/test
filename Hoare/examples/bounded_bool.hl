// Shows that b => i <= 10 is an inductive invariant.
vars int i;
vars bool b;

pre { true }

i = 2;
b = true;

while (b)
invariant { b => i <= 10 }
{
  i = i + 1;
  if (i < 1 || i > 10) {
    b = false;
  } else {
    skip;
  }
}

post { !b && (b => i <= 10) }
