// The invariant captures the quantity preserved by the loop body.
vars int k, m, n;

pre { k >= 0 }

if (k > 0) {
  n = k;
} else {
  n = -k;
}

m = -n;

while (m + n != 2 * k)
invariant { n - m == 2 * k }
{
  m = m + 1;
  n = n + 1;
}

post { m == 0 }
