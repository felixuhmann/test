# Binary Search

{0 <= target && target <= n}
lo := 0
hi := n
while lo < hi do
    mid := (lo + hi) / 2
    if mid < target then
        lo := mid + 1
    else
        hi := mid
    fi
od
{lo == target}
