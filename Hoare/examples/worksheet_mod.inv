vars nat a, b, c;

pre { true }

init {
  c = b % 2;
  a = b + c;
}

while (b > 0) {
  b = b - 1;
  a = a + 1;
  c = c + 1;
}

reachable { (a - b <= 2 * c) && ((b + c) % 2 = 0) }

candidate { a - b <= 2 * c }
candidate { (b + c) % 2 <= a % 2 }
candidate { a - b = 2 * c }
